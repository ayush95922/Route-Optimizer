spring.application.name=smartroute-backend
server.port=8080

# MongoDB
spring.data.mongodb.uri=mongodb+srv://ayush:ayush2468@cluster0.g1vqtl8.mongodb.net/pathoptimizer?retryWrites=true&w=majority&appName=Cluster0

# JWT
app.jwt.secret=SmartRouteIndoreTransit2026SuperSecretKeyForJWTSigning
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500,http://localhost:5500

# Disable Spring Security default login page (we handle auth ourselves)
spring.security.user.name=admin
spring.security.user.password=admin

logging.level.com.smartroute=DEBUG
